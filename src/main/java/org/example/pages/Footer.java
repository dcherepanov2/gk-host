package org.example.pages;

import org.openqa.selenium.WebDriver;

class Footer {// не реализовывал т.к. задание было заложить архитектуру
    private final WebDriver driver;

    Footer(WebDriver driver) {
        this.driver = driver;
    }
}
